---
import BandMemberCard from "@/components/BandMemberCard.astro";
import TextGradient from "@/components/ui/TextGradient.astro";
import { members } from "@/data/data";
---

<section
  class="w-full py-20 bg-gradient-to-b from-black to-zinc-900 relative overflow-hidden"
>
  <div class="container mx-auto px-4 relative z-10">
    <h2 class="text-4xl md:text-5xl font-serif text-center mb-12">
      <TextGradient isLong isAnimated> Nuestros Músicos </TextGradient>
    </h2>
    <div
      class="grid justify-items-center grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8"
    >
      {
        members.map((member, index) => (
          <BandMemberCard key={index} {...member} />
        ))
      }
    </div>
  </div>
</section>
